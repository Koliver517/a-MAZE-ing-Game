var map = [];


//loads up how the game will be scaled and sized ccoriding to the windows height and width
var w = window;

var tempWidth = w.innerWidth,
    tempHeight = w.innerHeight;

var width = tempWidth - 16,
    height = tempHeight - 16;

console.log(width);
console.log(height);
